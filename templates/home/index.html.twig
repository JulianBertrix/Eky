{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}
    <div style='height : 90vh;'>
        <div class="my-top">
            <div class="container">
                <h3 class="text-center text-white" style='margin-top: 25vh;'>Mon texte</h3>
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
        </div>
    </div>
    <div class=''>
        <h1>dfjdsfdlsfhdslkg</h1>
    </div>
{% endblock %}
