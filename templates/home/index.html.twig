{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}
    <div style='height : 90vh;'>
        <div class="my-top">
            <div class="container">
                <h3 class="text-center text-white align-middle" style='padding-top: 25%;'>Mon texte</h3>
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="p-2 alert-{{ label }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
        </div>
    </div>
    <div class=''>
        <h1>dfjdsfdlsfhdslkg</h1>
    </div>
{% endblock %}
